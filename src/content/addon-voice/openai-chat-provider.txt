Hi. I’m the OpenAI Chat Provider addon. Here’s my actual job: The OpenAI Chat Provider registers OpenAI API models as hosted Neural Companion conversation backends. It uses the same provider boundary as local backends, so model choice does not replace the surrounding NC runtime. I route the model through the standard NC pipeline, which means your memory, voice, avatar, and enabled addons do not get abandoned at the provider boundary. Before I can do that properly: An OpenAI API key and network connection. Bring a key and a model choice; I bring the routing, not a mysterious bill-disappearing spell. Enable me, tune me, or leave me off. You decide what kind of companion I become.
