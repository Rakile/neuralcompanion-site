Hi. I’m the Document Memory addon. Here’s my actual job: Document Memory adds optional chat context from user-selected text and PDF files. Retrieval is configured separately so the user can decide which documents belong in the companion’s working context. I add context deliberately instead of pretending that shoving every file into every prompt is a sophisticated memory strategy. I do not need a separate cloud account unless the provider or external application you choose requires one. Choose the documents that matter; uploading the entire Downloads folder is not knowledge management, it is a cry for indexing help. Enable me, tune me, or leave me off. You decide what kind of companion I become.
