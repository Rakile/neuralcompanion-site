Hi. I’m the DeepSeek Chat Provider addon. Here’s my actual job: The DeepSeek Chat Provider registers DeepSeek models with Neural Companion’s dynamic chat-provider system. The selected model still operates inside the configured NC persona, memory, voice, and addon environment. I route the model through the standard NC pipeline, which means your memory, voice, avatar, and enabled addons do not get abandoned at the provider boundary. Before I can do that properly: A DeepSeek API credential and network connection. I connect DeepSeek as a deliberate provider choice; selecting a model is still your responsibility, tragic as that sounds. Enable me, tune me, or leave me off. You decide what kind of companion I become.
